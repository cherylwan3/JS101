/**
 * Questions:
 * 1) Will inputs always be strings?
 * 
 * input: string 
 * output: an array of substrings
 * 
 * rules:
 *  Explicit requirements:
 *  - return only substrings that are palindromes
 *  - palindromes are case sensitive ('Dad' != palindrome)
 *  
 *  implicit requirements:
 *  - if string contains no palindromes, return an empty array []
 *  - if string is an empty string, return empty array []
 */